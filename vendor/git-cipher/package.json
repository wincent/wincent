{
  "name": "git-cipher",
  "version": "2.0.0",
  "bin": "./lib/main.mjs",
  "files": [
    "docs",
    "lib"
  ],
  "description": "Manages encrypted content in a Git repo",
  "exports": null,
  "repository": "https://github.com/wincent/git-cipher",
  "author": "Greg Hurrell <greg@hurrell.net>",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^24.1.0",
    "dprint": "^0.50.1",
    "esbuild": "^0.25.8",
    "typescript": "5.9.2"
  },
  "scripts": {
    "build": "esbuild src/main.mts --bundle --format=esm --platform=node --sourcemap --outfile=lib/main.mjs",
    "clean": "rm -r lib/*",
    "compile": "tsc --noEmit",
    "format:check": "dprint check",
    "format": "dprint fmt"
  },
  "type": "module",
  "engines": {
    "node": ">=20.0.0"
  }
}
