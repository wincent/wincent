#!/bin/zsh

source $HOME/.zsh/functions

if git root test -x script/generate_service_files.rb 2> /dev/null; then
  git root script/generate_service_files "$@"
else
  echo "error: couldn't find script/generate_service_files"
  exit 1
fi
