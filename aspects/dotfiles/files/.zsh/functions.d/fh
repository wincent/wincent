# fh - "find [in] history"
# Inspired by: https://github.com/junegunn/fzf/wiki/examples#command-history
function fh() {
  print -z $(fc -l 1 | sed 's/ *[0-9]*\*\{0,1\} *//' | tac | awk '!seen[$0]++' | tac | sk --no-multi --tiebreak=-index -q "$*" -p "❯ ")
}
