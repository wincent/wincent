" Defaults to off when running as root, and in a Codespace container, we're
" root.
set modeline

" Pull across non-root settings from: ~/.config/nvim/init.lua
set shada='0,<0,f0,n~/.config/nvim/shada
set undodir=~/.config/nvim/undo//,.
set undofile
