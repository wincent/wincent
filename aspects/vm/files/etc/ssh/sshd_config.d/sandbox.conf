# Locale only. API keys must not arrive via SendEnv; the guest reaches
# host nono-proxy over ssh -R and receives phantom tokens in the remote
# command environment instead.
AcceptEnv LANG LC_*
AllowTcpForwarding yes
StreamLocalBindUnlink yes
