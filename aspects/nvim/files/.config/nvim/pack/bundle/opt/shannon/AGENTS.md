# Formatting

- Use spaces for indentation, not tabs.
- Check for formatting violations with `bin/check-format`.
- Fix formatting with `bin/format`.

# Other checks

- When available, use `shellcheck` to check for potential problems in shell scripts.
