#!/usr/bin/env node

/*
 * Format with: prettier --write karabiner.js
 */

const assert = require('assert');
const {
  bundleIdentifier,
  deepCopy,
  isObject,
} = require('./karabiner');

(() => {
  assert(true, 'blinking light demo');
})();
