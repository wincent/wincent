source $LOCAL_ADMIN_SCRIPTS/vim/biggrep.vim

finish

" Disable check on save; too annoying.
"let g:fb_hack_on = 0

" Don't pollute my runtimepath with dozens of random files.
let g:fb_source_admin = 0

" Don't mess with my options.
let g:fb_default_opts = 0

" Don't mess with my files.
let g:fb_kill_whitespace = 0

" So deoplete can find system Python install.
let g:python3_host_prog='/usr/bin/python3'

source $LOCAL_ADMIN_SCRIPTS/master.vimrc
